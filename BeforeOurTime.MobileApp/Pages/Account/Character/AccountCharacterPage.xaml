<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:BeforeOurTime.MobileApp.Converters"
             x:Class="BeforeOurTime.MobileApp.Pages.Account.Character.AccountCharacterPage"
             Title="Characters">
    <ContentPage.Resources>
        <converters:NotBooleanConverter x:Key="inverter" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Margin="10">
            <StackLayout IsVisible="{Binding Working}" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
                <ActivityIndicator Color="Blue" IsRunning="{Binding Working}" IsVisible="{Binding Working}" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"></ActivityIndicator>
            </StackLayout>
            <StackLayout IsVisible="{Binding Working, Converter={StaticResource inverter}}"
                         VerticalOptions="FillAndExpand"
                         HorizontalOptions="FillAndExpand">
                <ListView 
                    x:Name="CharacterListView"
                    IsVisible="True"
                    ItemsSource="{Binding Characters}" 
                    ItemSelected="CharacterListView_ItemSelected"
                    VerticalOptions="FillAndExpand"
                    HorizontalOptions="FillAndExpand"
                    Margin="0, 10, 0, 0">
                    <ListView.Header>
                        <Label Text="Select Character to Play" 
                               FontSize="Medium"
                               Margin="0, 0, 0, 10"/>
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Frame BorderColor="LightGray" Margin="0" Padding="4" VerticalOptions="FillAndExpand" >
                                    <StackLayout BackgroundColor="WhiteSmoke" 
                                                 Orientation="Horizontal"
                                                 VerticalOptions="FillAndExpand"
                                                 HorizontalOptions="FillAndExpand"
                                                 Padding="0">
                                        <StackLayout Orientation="Vertical" Padding="0" Margin="0" VerticalOptions="CenterAndExpand" HorizontalOptions="Start">
                                            <Label LineHeight="-1" HeightRequest="16" FontAttributes="Bold" Margin="-1" Text="{Binding CharacterItem.Visible.Name}"></Label>
                                            <Label LineHeight="-1" HeightRequest="16" Margin="-2" Text="{Binding CharacterItem.Visible.Description}"></Label>
                                        </StackLayout>
                                        <StackLayout IsVisible="{Binding IsSelected}" 
                                                     Orientation="Horizontal"
                                                     HorizontalOptions="EndAndExpand"
                                                     VerticalOptions="CenterAndExpand">
                                            <Button Text="Play"
                                                HorizontalOptions="Start"
                                                VerticalOptions="CenterAndExpand"
                                                Margin="0, 0, 6, 0"
                                                WidthRequest="100"
                                                BackgroundColor="LightGreen"
                                                CornerRadius="10"
                                                BorderColor="Black" />
                                            <Button Text="Edit"
                                                HorizontalOptions="Start"
                                                VerticalOptions="CenterAndExpand"
                                                Margin="0, 0, 6, 0"
                                                WidthRequest="100"
                                                BackgroundColor="LightYellow"
                                                CornerRadius="10"
                                                BorderColor="Black" />
                                        </StackLayout>
                                    </StackLayout>
                                </Frame>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>